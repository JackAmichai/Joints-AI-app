package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Premium Dark Clinical Design System ───
// A sophisticated medical-grade dark palette inspired by surgical dashboards and health tech

// Backgrounds — Deep navy/charcoal layered surfaces
val SlateBg = Color(0xFF0B1120)        // Deep midnight navy — primary background
val SlateBgElevated = Color(0xFF111B2E) // Slightly elevated surface layer
val SlateCard = Color(0xFF162233)       // Card surface — frosted dark panel
val SlateCardElevated = Color(0xFF1C2D42) // Elevated card (modals, dialogs)
val SlateBorder = Color(0xFF263B56)     // Subtle border lines — steel blue

// Text Hierarchy — Clean silver/white progression
val TextTitle = Color(0xFFF1F5F9)       // Primary titles — near-white
val TextBody = Color(0xFF94A3B8)        // Body text — cool silver-gray
val TextMuted = Color(0xFF64748B)       // Muted labels — subdued slate
val TextOnAccent = Color(0xFF0B1120)    // Text on bright accent surfaces

// Primary Accent — Luminous teal-cyan gradient family
val ClinicalTeal = Color(0xFF00BCD4)    // Core accent — vibrant clinical teal
val ClinicalTealLight = Color(0xFF00E5FF) // Light variant — electric cyan glow
val ClinicalTealDark = Color(0xFF0097A7)  // Dark variant — deep teal anchor
val ClinicalTealSurface = Color(0x1A00BCD4) // 10% opacity teal for surfaces

// Secondary Accent — Soft mint/emerald
val MintSecondary = Color(0xFF4DB6AC)   // Secondary highlight — gentle mint
val MintSurface = Color(0x1A4DB6AC)     // 10% opacity mint for surfaces

// Status Colors — Calibrated for dark backgrounds
val CoralAlarm = Color(0xFFFF5252)      // Critical alert — luminous coral red
val CoralAlarmSurface = Color(0x1AFF5252) // 10% opacity red surface
val GreenSuccess = Color(0xFF69F0AE)    // Success state — neon green
val GreenSuccessSurface = Color(0x1A69F0AE) // 10% opacity green surface
val AmberWarning = Color(0xFFFFD740)    // Warning caution — electric amber
val AmberWarningSurface = Color(0x1AFFD740) // 10% opacity amber surface

// Glassmorphism & Depth
val GlassWhite = Color(0x0DFFFFFF)      // 5% white overlay for glass effect
val GlassBorder = Color(0x1AFFFFFF)     // 10% white for glass borders
val GlowTeal = Color(0x3300BCD4)        // Outer glow for accent elements
val ShadowDark = Color(0x66000000)      // Shadow overlay
